export default function DoctorsDashboardLayout({
    children,
}: Readonly<{
    children: React.ReactNode;
}>) {
    return (
        <>
            patient dashboard layout
            {children}
        </>
    );
}